# Loop example
var make_counter
    closure
        parameter max
        var counter 0
        return
            closure
                when
                    >= counter max
                    return false # return false after reaching `max`
                set counter
                    add counter 1
                return counter

var counter
    make_counter 10000

while i
    counter
    println i